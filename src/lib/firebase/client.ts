// Firebase client SDK is not configured in this deployment.
// Analytics and client-side Firebase features are disabled.
export const firebaseApp = null;
export const getAnalyticsSafe = async () => null;
